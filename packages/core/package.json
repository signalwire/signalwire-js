{
  "name": "@signalwire/core",
  "description": "Shared code for the SignalWire JS SDK",
  "author": "SignalWire Team <open.source@signalwire.com>",
  "license": "MIT",
  "private": true,
  "version": "0.0.0",
  "source": "src/index.ts",
  "esmodule": "./dist/index.modern.mjs",
  "type": "commonjs",
  "main": "dist/node/core.js",
  "exports": {
    ".": {
      "node": {
        "module": "./dist/node/core.esm.js",
        "require": "./dist/node/core.js"
      },
      "default": "./dist/web/core.modern.mjs"
    },
    "./package.json": "./package.json"
  },
  "module": "./dist/node/core.esm.js",
  "files": [
    "dist",
    "src"
  ],
  "engines": {
    "node": ">=10"
  },
  "scripts": {
    "start": "microbundle watch -f modern,es --define process.env.NODE_ENV=development --tsconfig tsconfig.build.json",
    "build": "npm run build:node && npm run build:web",
    "build:web": "pkg-typings --pre-run && microbundle --external none -f modern,es,cjs --define process.env.NODE_ENV=production --tsconfig ./tsconfig.build.json --output dist/web/ --target web && pkg-typings --after-run",
    "build:node": "pkg-typings --pre-run && microbundle -f modern,es,cjs --define process.env.NODE_ENV=production --tsconfig ./tsconfig.build.json --output dist/node/ --target node && pkg-typings --after-run",
    "test": "NODE_ENV=test jest",
    "prepare": "npm run build"
  },
  "dependencies": {
    "@reduxjs/toolkit": "^1.5.1",
    "loglevel": "^1.7.1",
    "redux-saga": "^1.1.3",
    "uuid": "^8.3.2"
  },
  "devDependencies": {
    "@types/uuid": "^8.3.0",
    "eventemitter3": "4.0.7",
    "jest-websocket-mock": "^2.2.0",
    "mock-socket": "^9.0.3",
    "redux-saga-test-plan": "^4.0.1",
    "strict-event-emitter-types": "^2.0.0"
  },
  "types": "dist/core/src/index.d.ts"
}
