{
  "name": "@signalwire/core",
  "version": "0.0.0",
  "source": "src/index.ts",
  "main": "dist/index.js",
  "module": "dist/core.esm.js",
  "types": "dist/core/src/index.d.ts",
  "files": [
    "dist",
    "src"
  ],
  "engines": {
    "node": ">=10"
  },
  "scripts": {
    "start": "tsdx watch --tsconfig tsconfig.build.json --verbose --noClean",
    "build": "microbundle --external none --define process.env.NODE_ENV=production --tsconfig ./tsconfig.build.json --target node",
    "test": "tsdx test",
    "lint": "tsdx lint",
    "prepare": "tsdx build"
  },
  "dependencies": {
    "@reduxjs/toolkit": "^1.5.1",
    "loglevel": "^1.7.1",
    "redux-saga": "^1.1.3",
    "uuid": "^8.3.2"
  },
  "devDependencies": {
    "@types/uuid": "^8.3.0",
    "jest-websocket-mock": "^2.2.0",
    "mock-socket": "^9.0.3"
  }
}
