export const validateRequest = (
  privateKey: string,
  header: string,
  requestUrl: string,
  rawBody: string
) => {
  console.log('Validate Request with', privateKey, header, requestUrl, rawBody)
}
